# Compiled artifacts
target/

# IDE
.idea/
*.iml
.vscode/
.settings/
.project
.classpath
.factorypath

# OS
.DS_Store

# Logs
*.log

# Local config / secrets (never commit real keys)
.env
application-local.yml

# Runtime persistence (chat memory DB + RAG vector store)
data/
.aider*

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# unlazy gate ledgers (scratch, per-plan)
.unlazy/

# annotation-processor output leaked into the project root by a build
org/
